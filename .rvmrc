if [[ -n "$rvm_path/environments" && -s "$rvm_path/environments/ruby-1.8.7-p302@bounce" ]] ; then
  \. "$rvm_path/environments/ruby-1.8.7-p302@bounce"
else
  rvm --create use  "ruby-1.8.7-p302@itslog"
fi
